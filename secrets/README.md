#### Store the secret keys in this folder. The file name format is as follows.
#### this secrets/* directory is ignored by .gitignore, except README.md

- claude-key [claude-api secret key to use shell-gpt with claude api]
